'use strict'

// Compatibility stub. Sidebar affixing is disabled in the compiled ShokaX
// source by patch-shokax-waline.js, so the browser needs no scroll listener or
// MutationObserver that can fight the theme's own scroll handler.
